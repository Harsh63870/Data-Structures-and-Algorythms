#include <bits/stdc++.h>
using namespace std;

/* 
1. taking set data structure and it will follow the rules .
*/
/*
PSeudocode
1. Brute Force
for(int i=0;i<n;i++){
set.insert(arr[i]);}
sam for other set
now 
for(Auto it in set){
cout<< it <<" "};

2. Two Pointer Approach
declare unione array[n]{
}
*/
vector <int> sortedArray(vector <int> nums1, vector<int> nums2){
    vector<int> unionarr;
    int a = nums1.size();
    int b = nums2.size();
    int i = 0;
    int j = 0;
    while(i < a && j < b){
        if(nums1[i]<=nums2[j]){
               
        }
    }
}